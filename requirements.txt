# Data & ML
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
transformers==4.41.2
torch==2.3.1
datasets==2.19.2
accelerate==1.14.0  # required by transformers.Trainer for distilbert training in mac

# Kafka
confluent-kafka==2.4.0

# Spark (PySpark)
pyspark==3.5.1

# MLflow
mlflow==2.13.0
setuptools<81  # pin: mlflow 2.13.0 imports pkg_resources, removed from setuptools>=81

# DVC
dvc==3.51.2
pathspec==0.12.1  # pin: dvc otherwise throws error with wrong pathspec
dvc-gdrive==3.0.1  # needed for our Google Drive remote (dvc push/pull)
protobuf<5  # pin: dvc-gdrive pulls in protobuf 7.x which conflicts with mlflow's protobuf<5 requirement
pyOpenSSL==24.2.1  # pin: dvc-gdrive/PyDrive2 otherwise resolves an incompatible cryptography version, breaking OpenSSL.crypto (lib.GEN_EMAIL AttributeError)

# Serving
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# Testing
pytest==8.2.2
